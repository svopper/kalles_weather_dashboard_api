{
    "configurations": [
        {
            "type": "go",
            "request": "launch",
            "name": "Launch Program",
            "mode": "auto",
            "program": "${workspaceFolder}/${input:programPath}",
            "cwd": "${workspaceFolder}"
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "programPath",
            "description": "Path to the program folder or Go file to debug"
        }
    ]
}